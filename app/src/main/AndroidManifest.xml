<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <queries>
        <package android:name="com.abk.kernel" />
    </queries>

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity
            android:name=".FidoDiagnosticsActivity"
            android:exported="true"
            android:label="@string/diagnostics_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".FidoBootstrapActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:finishOnTaskLaunch="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="com.abk.kernel.action.ABK_EXTENSION" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="com.abk.kernel.extension.ID"
                android:value="abk_fido_store" />
            <meta-data
                android:name="com.abk.kernel.extension.NAME"
                android:value="ABK FIDO Companion" />
            <meta-data
                android:name="com.abk.kernel.extension.OOBE_ACTIVITY"
                android:value=".FidoBootstrapActivity" />
            <meta-data
                android:name="com.abk.kernel.extension.SETTINGS_ACTIVITY"
                android:value=".FidoBootstrapActivity" />
            <meta-data
                android:name="com.abk.kernel.extension.SERVICE_ACTIVITY"
                android:value=".FidoSyncService" />
        </activity>

        <activity
            android:name=".FidoAuthPromptActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:finishOnTaskLaunch="true"
            android:launchMode="singleInstance"
            android:theme="@style/Theme.AbkFido.Translucent" />

        <receiver
            android:name=".SyncTriggerReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_UNLOCKED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.ACTION_SHUTDOWN" />
            </intent-filter>
        </receiver>

        <service
            android:name=".FidoSyncService"
            android:directBootAware="true"
            android:exported="true"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".FidoKeepAliveJobService"
            android:directBootAware="true"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <provider
            android:name=".FidoInitProvider"
            android:authorities="${applicationId}.init"
            android:directBootAware="true"
            android:exported="false" />
    </application>

</manifest>
